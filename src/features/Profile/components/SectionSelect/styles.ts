import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  image: {
    width: 60,
    height: 60,
    borderRadius: 48,
  },
  text: {
    fontSize: 18,
    color: '#000',
  },
});
